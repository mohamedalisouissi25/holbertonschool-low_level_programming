#include <stdio.h>
#include <stdlib.h>

/**
* create_array - create array
*
* @size: input
* @c: input
* Return: output
*/
char *create_array(unsigned int size, char c)
{
char *a;
unsigned int i;

a = malloc(sizeof(char) * a);

	if (s <= 0 || a == NULL)
	return (NULL);
	for (i = 0; i < a; i++)
	{
	a[i] = c;
	}

return (a);
}
