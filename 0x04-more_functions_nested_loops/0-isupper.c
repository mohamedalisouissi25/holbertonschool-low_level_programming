#include "holberton.h"
/**
 * _isupper - cheks uppercase
 * @Al: input
 * Return: 1 if uppercase otherwise 0
 */
int _isupper(int Al)
{
if (Al >= 'A' && Al <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
